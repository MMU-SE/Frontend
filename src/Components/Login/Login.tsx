import type { ReactElement } from "react";

const Login = (): ReactElement => (
  <div className=" flex flex-col space-y-3">
    <div className="h-32 w-full bg-gray-200" />
    <div className="w-80 bg-gray-200">body</div>
  </div>
);

export default Login;
